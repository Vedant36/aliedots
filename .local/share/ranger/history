shell git clone https://github.com/Vedant36/dotfiles
mkdir dox
shell git clone https://github.com/Vedant36/textfiles
shell git clone https://github.com/Vedant36/scripts bin
mkdir plugins
mkdir opt
shell git clone https://git.suckless.org/dwm
shell git clone https://git.suckless.org/dmenu
find fz
shell cp /etc/xdg/reflector/ .
shell cp -r /etc/xdg/reflector/ .
shell dot add %s
mkdir q
mkdir melum
mkdir pix
mkdir dl
mkdir vids
mkdir var
mkdir log
rename cache
shell git clone gl:CPlus
shell git init --bare --git-dir asd
shell git init --bare --seperate-git-dir asd
shell git init --bare --separate-git-dir asd
shell git init --bare
rename archbtw.png
filter pass
mkdir udev
mkdir rules
shell cp /etc/udev/rules.d/* .
rename rules.d
rename .pkisjdakjsd
mkdir tmp
mkdir desktop
mkdir templates
mkdir public
linemode 
linemode sizemtime

mkdir patches
shell sudo make clean install
rename config.h
shell cp /etc/pacman.conf .
shell 
find udev
find ude
delete
mkdir lib
mkdir vn36/wrappers
rename aliedots
